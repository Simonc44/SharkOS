# =============================================================================
# Module custom Calamares — relance du cycle SharkOS dans le chroot
# =============================================================================
---
type: "shellprocess"

command: "/usr/local/bin/sharkos-install-cycle.sh"

timeout: 1800   # 30 min max

chroot: true

needs:
    - unpackfs

weight: 1000
---
